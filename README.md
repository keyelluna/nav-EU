
  # Launch Page with Animation

  This is a code bundle for Launch Page with Animation. The original project is available at https://www.figma.com/design/zCslWHzvkHwpt7HsIbrrcp/Launch-Page-with-Animation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  